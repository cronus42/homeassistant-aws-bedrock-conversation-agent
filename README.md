# AWS Bedrock Conversation for Home Assistant

This repository provides a **custom integration** that lets Home Assistant use Amazon Bedrock models (Claude, Llama, Mistral) as conversation agents with device control via Home Assistant’s conversation system.

For end-user setup and detailed feature documentation, see `custom_components/bedrock_conversation/README.md`.
